# AI 工具与模型清单每日任务

面向 AI 执行。按任务顺序推进，每个任务都必须输出明确文件。  
当天日期默认使用 `20260611`，后续每天执行时替换为当天日期。

## 总范围

- 只聚焦两个领域：
  - Agent Tool
  - Top 模型
- 国外公司不超过 7 家。
- 国内公司不超过 7 家。
- Agent Tool 不超过 15 个。
- Top 模型不超过 15 个。
- 只采信官方来源：官网、官方文档、官方博客、官方 API 文档、官方 release notes。
- 未公开底层模型时写 `undisclosed`，不要猜测。

## 公司范围

### 国外公司，不超过 7 家

1. OpenAI
2. Anthropic
3. Google DeepMind
4. Meta
5. xAI
6. Mistral AI
7. Perplexity

### 国内公司，不超过 7 家

1. DeepSeek
2. 阿里巴巴 / Qwen
3. Moonshot / Kimi
4. 字节跳动 / 豆包 / Seed
5. MiniMax
6. 智谱 / Z.ai
7. 百度文心

## 任务 1：生成公司清单

Prompt：

```text
按公司范围检索官方来源，整理国内外 AI 公司清单。只保留 Agent Tool 或 Top 模型相关公司。
```

任务要求：

- 每家公司必须有官网或官方文档。
- 每家公司写清楚所属地区、公司类型、核心 AI 产品方向。
- 不扩展到长尾公司。
- 国外最多 7 家，国内最多 7 家。

产物：

- `公司/公司清单 20260611.md`

文件结构：

```text
# 公司清单 20260611

## 国外公司

| 公司 | 国家/地区 | 类型 | 相关领域 | 官方来源 | 备注 |
| --- | --- | --- | --- | --- | --- |

## 国内公司

| 公司 | 国家/地区 | 类型 | 相关领域 | 官方来源 | 备注 |
| --- | --- | --- | --- | --- | --- |
```

## 任务 2：生成 Top 模型清单

Prompt：

```text
基于任务 1 的公司清单，检索官方模型列表，整理当前主推或最新 Top 模型，不超过 15 个。
```

任务要求：

- 只收录官方当前主推、最新发布、或仍在官方模型列表中可用的模型。
- 不做能力排行榜。
- 不引用非官方榜单作为模型依据。
- 每个模型至少有 1 个官方来源。
- 总数不超过 15 个。

产物：

- `模型/Top模型清单 20260611.md`

文件结构：

```text
# Top 模型清单 20260611

| 模型 | 公司 | 模态 | 定位 | 开放方式 | 适合场景 | 官方来源 | 核验日期 |
| --- | --- | --- | --- | --- | --- | --- | --- |
```

字段说明：

- `模态`：文本 / 图像 / 视频 / 音频 / 多模态 / 3D。
- `定位`：旗舰 / 轻量 / 推理 / 编码 / 搜索 / 视频生成 / 图像生成。
- `开放方式`：API / App / 开源权重 / 企业私有化 / 限定开放 / unknown。

## 任务 3：生成 Agent Tool 清单

Prompt：

```text
基于任务 1 的公司清单，检索官方产品页，整理 Agent Tool 清单，不超过 15 个。
```

任务要求：

- 只收录能完成多步骤任务、代码开发、工作流编排、浏览器/文件/知识库操作的 AI 工具。
- 不收录普通聊天工具，除非官方明确有 Agent、Code、Workflow、Automation 能力。
- 底层模型未公开时写 `undisclosed`。
- 每个工具至少有 1 个官方来源。
- 总数不超过 15 个。

产物：

- `工具/Agent工具清单 20260611.md`

文件结构：

```text
# Agent 工具清单 20260611

| 工具 | 公司 | 工具类型 | 核心能力 | 底层模型/供应商 | 适合岗位 | 官方来源 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
```

工具类型可选：

- 编码 Agent
- 通用任务 Agent
- 企业 Agent
- 工作流编排
- 开发框架
- 浏览器 Agent
- 办公协同 Agent

## 任务 4：生成公司详情卡

Prompt：

```text
基于前三个清单，为每家公司生成详情卡，只写与 Agent Tool 和 Top 模型相关的信息。
```

任务要求：

- 每家公司一张详情卡。
- 只写已核验信息。
- 不补写商业判断。
- 不扩写到无关产品线。

产物：

- `公司/公司详情卡 20260611.md`

文件结构：

```text
# 公司详情卡 20260611

## 公司名

- 地区：
- 类型：
- 官网：
- 官方文档：
- 核心方向：
- 代表 Top 模型：
- 代表 Agent Tool：
- 适合岗位：
- 风险备注：
- 核验日期：
```

## 任务 5：生成岗位推荐清单

Prompt：

```text
基于 Top 模型清单和 Agent 工具清单，按岗位生成推荐组合，只使用已收录条目。
```

任务要求：

- 不新增未在任务 2、任务 3 出现过的模型或工具。
- 每个岗位推荐 2-5 个工具或模型。
- 推荐理由要短。
- 不做价格比较。
- 不做绝对排名。

产物：

- `推荐/岗位推荐清单 20260611.md`

文件结构：

```text
# 岗位推荐清单 20260611

| 岗位 | 推荐工具/模型 | 推荐理由 | 使用注意 |
| --- | --- | --- | --- |
```

岗位范围：

1. 研发
2. 产品
3. 设计
4. 运营
5. 市场
6. 管理者

## 任务 6：生成来源索引

Prompt：

```text
汇总本轮所有官方来源，按公司、模型、工具建立来源索引。
```

任务要求：

- 只记录官方来源。
- 每条来源写清楚对应对象。
- URL 不要重复堆叠；同一来源可复用。

产物：

- `来源/官方来源索引 20260611.md`

文件结构：

```text
# 官方来源索引 20260611

| 对象类型 | 对象名称 | 公司 | 来源名称 | URL | 用途 |
| --- | --- | --- | --- | --- | --- |
```

## 任务 7：生成校验报告

Prompt：

```text
检查本轮产物是否满足数量、来源、字段和引用关系要求，输出校验报告。
```

任务要求：

- 检查国外公司是否不超过 7 家。
- 检查国内公司是否不超过 7 家。
- 检查 Top 模型是否不超过 15 个。
- 检查 Agent Tool 是否不超过 15 个。
- 检查每家公司是否有官网或官方文档。
- 检查每个模型是否有官方来源。
- 检查每个工具是否有官方来源。
- 检查是否存在非官方来源。
- 检查是否存在未解释的 `unknown` 或 `undisclosed`。

产物：

- `校验/校验报告 20260611.md`

文件结构：

```text
# 校验报告 20260611

## 数量校验

## 来源校验

## 字段校验

## 引用关系校验

## 待复核问题

## 是否可进入页面开发
```

## 任务 8：生成当日汇总

Prompt：

```text
汇总当天所有产物，说明新增、更新、待复核和下一步任务。
```

任务要求：

- 不重复粘贴完整清单。
- 只总结变化和问题。
- 明确下一天应该优先检索什么。

产物：

- `日报/AI工具模型日报 20260611.md`

文件结构：

```text
# AI 工具模型日报 20260611

## 今日产物

## 新增内容

## 更新内容

## 待复核问题

## 明日任务
```

## 任务 9：写入 catalog-data.ts

Prompt：

```text
把本轮所有已校验结果写入 catalog-data.ts，作为后续网页开发的数据源。
```

任务要求：

- 这是最后一个任务，必须在校验报告通过后执行。
- 汇总任务 1 到任务 8 的有效结果。
- 直接生成或更新 `catalog-data.ts`，不再只生成草稿。
- 使用 TypeScript，不使用 JavaScript。
- 使用现有 `catalog-data.ts` 类型定义。
- 字段与 `company-first-ai-catalog-plan.md` 保持一致。
- `companies` 来自 `公司/公司清单 20260611.md` 和 `公司/公司详情卡 20260611.md`。
- `models` 来自 `模型/Top模型清单 20260611.md`。
- `tools` 来自 `工具/Agent工具清单 20260611.md`。
- `sourceUrls` 对齐 `来源/官方来源索引 20260611.md`。
- `companyId` 必须能匹配 `companies.id`。
- `modelProviderIds` 只能填写已确认的公司 ID。
- 未确认字段写 `unknown` 或 `undisclosed`。
- 保留不超过 7 家国外公司、7 家国内公司、15 个 Top 模型、15 个 Agent Tool。

产物：

- `catalog-data.ts`

文件结构：

```ts
export const companies: Company[] = [];
export const models: Model[] = [];
export const tools: Tool[] = [];
```

## 每日执行顺序

1. 任务 1：生成公司清单。
2. 任务 2：生成 Top 模型清单。
3. 任务 3：生成 Agent Tool 清单。
4. 任务 4：生成公司详情卡。
5. 任务 5：生成岗位推荐清单。
6. 任务 6：生成来源索引。
7. 任务 7：生成校验报告。
8. 任务 8：生成当日汇总。
9. 任务 9：写入 catalog-data.ts。

## 页面开发进入条件

只有满足以下条件，才进入 `index.html`、`styles.css`、`script.ts`、`catalog-data.ts` 页面开发：

- `校验/校验报告 20260611.md` 结论为可进入页面开发。
- 公司数量符合限制。
- Top 模型数量符合限制。
- Agent Tool 数量符合限制。
- 所有模型和工具都有官方来源。
- 未公开底层模型已标记为 `undisclosed`。
- `catalog-data.ts` 已生成或更新。
- `catalog-data.ts` 已包含公司、Top 模型、Agent Tool 和官方来源字段。
