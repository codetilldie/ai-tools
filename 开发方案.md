# AI Tools Catalog 单页网页开发方案

## 1. 目标

基于 `src/catalog-data.ts` 开发一个 PC 优先的单页网页，用来展示每日更新的 AI 公司、Top 模型、Agent Tool 和官方来源。

技术约束：

- 使用 `HTML + TypeScript + Tailwind CSS`。
- 源码不手写 JavaScript；交付构建时可以由 TypeScript 编译生成浏览器可执行 JS。
- `src/catalog-data.ts` 是唯一数据源，每日更新后页面自动适配新列表。
- 页面不硬编码公司、模型、工具、筛选项数量。
- 页面要支持动态列表、动态筛选、动态统计、空状态和来源追溯。

## 2. Product Design Brief

页面定位：

- 给培训、选型和内部研究使用的 AI 工具与模型目录。
- 用户先看整体格局，再按公司、模型、Agent Tool、岗位和来源快速筛选。
- 重点不是营销展示，而是高密度、可扫描、可复核的数据目录。

视觉方向：

- 专业、克制、数据产品风格。
- PC 优先，适配 `1366x768`、`1440x900`、`1920x1080`。
- 信息密度高，但要避免表格拥挤和横向滚动。
- 使用 Tailwind 建立稳定的设计 token：浅色背景、深色正文、蓝/青作为操作强调、琥珀色用于 `unknown` / `undisclosed` 风险提示。

交互级别：

- 完整交互。
- 搜索、筛选、排序、视图切换、重置、详情展开、来源查看都要可用。

## 3. 文件结构

建议结构：

```text
ai-tools/
├── index.html
├── src/
│   ├── script.ts
│   ├── catalog-data.ts
│   ├── styles.css
│   └── modules/
│       ├── data.ts
│       ├── filters.ts
│       ├── render.ts
│       └── url-state.ts
├── dist/
├── scripts/
│   └── deploy-gh-pages.sh
├── tailwind.config.ts
├── vite.config.ts
├── package.json
├── tsconfig.json
├── .gitignore
└── 开发方案.md
```

说明：

- `src/catalog-data.ts`：每日更新的数据源。
- `src/script.ts`：页面入口，负责初始化应用。
- `src/modules/`：页面渲染、筛选、排序、URL 状态、数据健康检查等模块。
- `src/styles.css`：Tailwind 入口样式和少量组件层样式。
- `dist/`：构建产物目录，只给 GitHub Pages 发布使用，不手工编辑。
- `scripts/deploy-gh-pages.sh`：将 `dist/` 发布到 `gh-pages` 分支。
- `tailwind.config.ts`：Tailwind 内容扫描和主题 token。
- `vite.config.ts`：Vite 构建配置和 GitHub Pages base path。
- `.gitignore`：忽略 `dist/`、`node_modules/`、本地缓存和临时文件。
- `index.html`：Vite 入口 HTML、页面挂载容器和静态 SEO 信息。

## 4. 数据接入方案

从 `src/catalog-data.ts` 导入：

```ts
import {
  catalogMeta,
  companies,
  models,
  tools,
  officialSources,
  type Company,
  type Model,
  type Tool,
} from "./catalog-data";
```

如果后续拆到 `src/modules/data.ts`，从模块内导入时使用：

```ts
import { companies, models, tools, officialSources } from "../catalog-data";
```

页面启动时生成派生数据：

```ts
const companyById = new Map(companies.map((company) => [company.id, company]));
const modelsByCompanyId = groupBy(models, "companyId");
const toolsByCompanyId = groupBy(tools, "companyId");
const sourceByUrl = new Map(officialSources.map((source) => [source.url, source]));
```

动态计算：

- 公司数量、国内公司数、国外公司数。
- Top 模型数量。
- Agent Tool 数量。
- 官方来源数量。
- 最近核验日期：从 `checkedAt` 最大值计算。
- 筛选选项：从真实数据中提取，不手写固定枚举列表。

## 5. 数据更新兼容规则

每日更新 `src/catalog-data.ts` 后，页面必须自动适配：

- 新公司出现时，自动进入公司列表和筛选项。
- 新模型出现时，自动进入模型列表，并关联公司。
- 新工具出现时，自动进入工具列表，并关联公司和模型供应商。
- 新 `toolType`、`modality`、`availability`、`audiences` 出现时，自动出现在筛选器。
- 字段为 `unknown` 时显示为弱风险状态。
- 字段包含 `undisclosed` 时显示为琥珀色风险标签。
- `companyId` 找不到公司时，页面显示“公司未匹配”，但不崩溃。
- `sourceUrls` 找不到来源索引时，仍展示 URL，并标记“未进入来源索引”。

需要实现运行时校验函数：

```ts
function validateCatalogData() {
  return {
    brokenModelCompanyIds: [],
    brokenToolCompanyIds: [],
    brokenProviderIds: [],
    missingSourceUrls: [],
  };
}
```

校验结果在页面底部“数据健康状态”中展示。

## 6. 页面信息架构

### 6.1 顶部区域

内容：

- 标题：`AI Agent Tools & Top Models Catalog`
- 副标题：说明数据来自官方来源，并每日更新。
- 最近核验日期。
- 数据范围说明：国外 ≤ 7 家、国内 ≤ 7 家、Top 模型 ≤ 15、Agent Tool ≤ 15。

统计卡：

- 公司数
- Top 模型数
- Agent Tool 数
- 官方来源数
- `unknown` 字段数
- `undisclosed` 工具数

### 6.2 全局控制区

控件：

- 全局搜索框：搜索公司、模型、工具、适合场景、备注。
- 视图切换：
  - 全部
  - 公司
  - Top 模型
  - Agent Tool
  - 来源
- 重置筛选按钮。
- 数据更新时间标签。

筛选器：

- 地区：国内 / 海外。
- 公司类型。
- 领域：Agent Tool / Top 模型。
- 模型模态。
- 模型定位。
- 开放方式。
- 工具类型。
- 适合岗位。
- 风险：全部 / unknown / undisclosed。

要求：

- 筛选项全部由数据动态生成。
- 每个筛选项显示匹配数量。
- 多筛选条件组合时实时更新列表。
- 筛选为空时显示明确空状态和重置按钮。

### 6.3 公司视图

展示方式：

- 卡片网格，PC 下 2-3 列。
- 每张卡展示公司、地区、类型、核心方向、代表模型、代表工具、核验日期。

卡片结构：

```text
公司名
地区 / 公司类型 / Focus Areas
summary
Top 模型：...
Agent Tool：...
风险备注
官网 / 文档 / 来源
```

交互：

- 点击公司卡进入右侧详情抽屉。
- 抽屉展示该公司全部模型、全部工具和全部来源。
- 点击来源 URL 新窗口打开。

### 6.4 Top 模型视图

展示方式：

- PC 使用表格 + 紧凑卡片混合布局。
- 宽屏显示表格，窄屏自动切为卡片。

字段：

- 模型名
- 公司
- 模态
- 定位
- 上下文窗口
- 开放方式
- 适合场景
- 核验日期
- 来源

交互：

- 按公司、模态、定位、开放方式筛选。
- 按公司名、模型名、核验日期排序。
- `unknown` 字段以风险标签显示。

### 6.5 Agent Tool 视图

展示方式：

- 按工具类型分组。
- 每组内使用工具卡片。

字段：

- 工具名
- 所属公司
- 工具类型
- 核心能力
- 底层模型 / 供应商
- 适合岗位
- 官方来源
- 备注

交互：

- 按岗位筛选。
- 按工具类型筛选。
- 按模型供应商筛选。
- `undisclosed` 工具高亮提醒。

### 6.6 来源视图

展示方式：

- 来源表格。
- 支持按对象类型筛选：公司 / 模型 / 工具。

字段：

- 来源名称
- 对象类型
- 对象名称
- 公司
- 用途
- URL
- 核验日期

交互：

- 搜索来源名称和 URL。
- 点击 URL 新窗口打开。
- 显示每个对象的来源覆盖状态。

### 6.7 数据健康状态

放在页面底部或来源视图尾部。

展示：

- 数据数量是否超过 `catalogMeta.scope` 限制。
- 是否存在断开的 `companyId`。
- 是否存在断开的 `modelProviderIds`。
- 是否存在没有来源的模型或工具。
- 是否存在未进入 `officialSources` 的 URL。

状态文案：

- 全部通过：绿色 `Data ready for page use`
- 有提醒：琥珀色 `Needs review`
- 有阻断：红色 `Blocked`

## 7. 状态管理

使用一个轻量状态对象，不引入框架：

```ts
type ViewMode = "all" | "companies" | "models" | "tools" | "sources";

type CatalogState = {
  query: string;
  viewMode: ViewMode;
  regions: string[];
  companyTypes: string[];
  focusAreas: string[];
  modalities: string[];
  positionings: string[];
  availabilities: string[];
  toolTypes: string[];
  audiences: string[];
  riskOnly: "all" | "unknown" | "undisclosed";
  sortBy: "name" | "company" | "checkedAt";
};
```

所有渲染函数都从 `CatalogState` 和 `src/catalog-data.ts` 派生数据，不直接修改原始数据。

URL 状态建议：

- 将 `query`、`viewMode`、关键筛选项同步到 `location.search`。
- 用户刷新页面后保留当前筛选条件。
- 分享链接时可复现当前视图。

## 8. 渲染模块拆分

`src/script.ts` 推荐拆成逻辑区块，后续可再拆文件：

```text
1. imports
2. type aliases
3. DOM refs
4. state
5. data normalization
6. filter option builders
7. filter functions
8. render functions
9. event bindings
10. URL state sync
11. init
```

核心函数：

```ts
renderApp()
renderStats()
renderFilters()
renderCompanyCards()
renderModelList()
renderToolGroups()
renderSourceTable()
renderDataHealth()
openCompanyDrawer(companyId)
resetFilters()
syncStateToUrl()
hydrateStateFromUrl()
```

## 9. Tailwind 设计规范

基础布局：

- 页面背景：`bg-slate-50`
- 主内容宽度：`max-w-7xl mx-auto px-6`
- 区块间距：`py-8`
- 卡片：`rounded-lg border border-slate-200 bg-white shadow-sm`
- 不使用大圆角，卡片圆角控制在 `rounded-lg` 或以下。

颜色：

- 主文本：`text-slate-950`
- 次文本：`text-slate-600`
- 边框：`border-slate-200`
- 主操作：`bg-cyan-600 text-white`
- Agent Tool：`bg-cyan-50 text-cyan-700`
- Top 模型：`bg-indigo-50 text-indigo-700`
- unknown：`bg-amber-50 text-amber-700`
- undisclosed：`bg-orange-50 text-orange-700`
- 错误：`bg-rose-50 text-rose-700`

排版：

- 页面标题：`text-3xl font-semibold tracking-normal`
- 区块标题：`text-xl font-semibold`
- 卡片标题：`text-base font-semibold`
- 表格文字：`text-sm`
- 标签文字：`text-xs font-medium`

PC 响应：

- `1366x768`：主区域不能横向滚动。
- `1440x900`：卡片网格保持 2-3 列。
- `1920x1080`：内容保持居中，最大宽度不超过 `max-w-7xl`。
- 长模型名和 URL 使用 `break-words` / `truncate`，不要撑破卡片。

## 10. 动态数据处理细节

### 10.1 筛选项生成

不要硬编码：

```ts
const modalityOptions = unique(models.map((model) => model.modality));
const toolTypeOptions = unique(tools.map((tool) => tool.toolType));
const audienceOptions = unique(tools.flatMap((tool) => tool.audiences));
```

### 10.2 关联公司

模型和工具展示公司名时：

```ts
function getCompanyName(companyId: string) {
  return companyById.get(companyId)?.name ?? "公司未匹配";
}
```

### 10.3 风险识别

```ts
function hasUnknown(value: unknown) {
  return String(value).toLowerCase().includes("unknown");
}

function hasUndisclosed(value: unknown) {
  return String(value).toLowerCase().includes("undisclosed");
}
```

### 10.4 空状态

所有列表都要支持空状态：

```text
没有匹配结果
请调整筛选条件或清空搜索
[重置筛选]
```

## 11. 开发步骤

### Step 1：搭建基础文件

- 新建 `index.html`
- 新建 `src/script.ts`
- 新建 `src/styles.css`
- 新建 `src/modules/`
- 新建 `tailwind.config.ts`
- 新建 `vite.config.ts`
- 将现有 `catalog-data.ts` 移入 `src/catalog-data.ts`

### Step 2：实现数据归一化

- `companyById`
- `modelsByCompanyId`
- `toolsByCompanyId`
- `sourceByUrl`
- 数据健康检查

### Step 3：实现页面骨架

- 顶部标题
- 统计卡
- 全局搜索
- 视图切换
- 筛选器容器
- 主内容容器
- 详情抽屉

### Step 4：实现公司视图

- 公司卡片
- 公司详情抽屉
- 公司关联模型和工具

### Step 5：实现模型视图

- 模型表格
- 模型卡片 fallback
- 模态、定位、开放方式筛选

### Step 6：实现工具视图

- 按工具类型分组
- 岗位筛选
- 底层模型供应商展示
- `undisclosed` 提醒

### Step 7：实现来源视图

- 来源表格
- 来源搜索
- 对象类型筛选
- URL 外链

### Step 8：实现状态同步

- 筛选状态更新
- URL query 同步
- 刷新后恢复状态
- 重置筛选

### Step 9：实现 Tailwind 视觉打磨

- 卡片布局
- 标签样式
- 表格样式
- 详情抽屉
- 空状态
- 数据健康状态

### Step 10：验证

- TypeScript 编译通过。
- Tailwind 样式生成正常。
- `src/catalog-data.ts` 更新后页面无需改代码即可刷新列表。
- `1366x768` 无横向滚动。
- `1440x900` 无卡片重叠。
- `1920x1080` 内容居中。
- 搜索和筛选组合正常。
- 空状态正常。
- 来源链接可打开。

### Step 11：发布到 GitHub Pages

发布方式：

- 使用 GitHub Pages 托管静态构建产物。
- 使用 `gh-pages` 分支发布，不使用 GitHub Actions workflow。
- Pages 产物目录使用 `dist/`，不要直接发布源码根目录。
- `src/catalog-data.ts` 每日更新后，执行 `npm run deploy:pages` 重新构建并发布页面。

构建约定：

```text
源码：
- index.html
- src/script.ts
- src/styles.css
- src/catalog-data.ts
- src/modules/*
- tailwind.config.ts
- vite.config.ts

构建输出：
- dist/index.html
- dist/assets/*
```

目录职责：

- `src/`：唯一源码目录，所有 TypeScript、Tailwind 入口样式、数据源都放这里。
- `dist/`：只由 `npm run build` 生成，禁止手工编辑。
- `.gitignore` 必须包含 `dist/`，避免构建产物进入源码提交。
- GitHub Pages 只发布 `dist/`。
- 每日更新数据时，只改 `src/catalog-data.ts`，不要改 `dist/`。

`package.json` 建议脚本：

```json
{
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "deploy:pages": "npm run typecheck && npm run build && sh scripts/deploy-gh-pages.sh"
  },
  "devDependencies": {
    "@vitejs/plugin-legacy": "latest",
    "tailwindcss": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
```

Vite 配置要求：

- 如果仓库名是 `ai-tools`，GitHub Pages 路径通常是 `/ai-tools/`。
- `vite.config.ts` 需要设置 `base: "/ai-tools/"`。
- 如果后续使用自定义域名，再调整 `base` 和 Pages 域名配置。

`vite.config.ts` 示例：

```ts
import { defineConfig } from "vite";

export default defineConfig({
  base: "/ai-tools/",
  root: ".",
  build: {
    outDir: "dist",
    emptyOutDir: true,
  },
});
```

`index.html` 入口要求：

```html
<div id="app"></div>
<script type="module" src="/src/script.ts"></script>
```

Tailwind 内容扫描：

```ts
export default {
  content: ["./index.html", "./src/**/*.{ts,tsx,html}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
```

`gh-pages` 发布脚本：

```sh
#!/usr/bin/env sh
set -eu

remote_url="$(git remote get-url origin)"
tmp_dir="$(mktemp -d)"
trap 'rm -rf "$tmp_dir"' EXIT

cp -R dist/. "$tmp_dir"/
touch "$tmp_dir/.nojekyll"

git -C "$tmp_dir" init -q
git -C "$tmp_dir" checkout -b gh-pages >/dev/null 2>&1
git -C "$tmp_dir" add -A
git -C "$tmp_dir" -c user.name="codex" -c user.email="codex@local" commit -m "Deploy GitHub Pages" >/dev/null
git -C "$tmp_dir" remote add origin "$remote_url"
git -C "$tmp_dir" push --force origin gh-pages
```

GitHub Pages 仓库设置：

- Repository Settings -> Pages。
- Source 选择 `Deploy from a branch`。
- Branch 选择 `gh-pages`。
- Folder 选择 `/ (root)`。
- 首次部署后记录 Pages URL。
- 预期 URL：`https://codetilldie.github.io/ai-tools/`。

发布验证：

- `npm run deploy:pages` 执行成功。
- Pages URL 返回 200。
- 首页标题、统计卡、公司列表、模型列表、工具列表正常渲染。
- 刷新任意筛选 URL 后状态能恢复。
- 浏览器控制台无阻断错误。
- `dist/` 不进入 `main` 源码提交，只由发布脚本推送到 `gh-pages` 分支。

## 12. 验收标准

功能验收：

- 能展示全部公司、模型、工具、来源。
- 能按地区、公司类型、模态、开放方式、工具类型、岗位筛选。
- 能搜索公司、模型、工具、场景、备注。
- 能切换视图。
- 能打开公司详情。
- 能看到数据健康状态。

数据验收：

- `src/catalog-data.ts` 中新增公司后，页面自动显示。
- `src/catalog-data.ts` 中新增模型后，模型筛选项自动更新。
- `src/catalog-data.ts` 中新增工具类型后，工具筛选项自动更新。
- 删除某个公司后，关联异常在数据健康状态中显示。
- `unknown` / `undisclosed` 有明显视觉提示。

发布验收：

- `npm run typecheck` 通过。
- `npm run build` 通过。
- `npm run deploy:pages` 发布成功。
- GitHub Pages URL 可访问。
- GitHub Pages 页面加载的是最新 `src/catalog-data.ts` 构建结果。
- 直接访问带 query 的筛选 URL 不出现 404。

视觉验收：

- PC 常见分辨率无横向滚动。
- 表格和卡片不重叠。
- 长文本不会撑破布局。
- 操作控件清晰，不像营销落地页。

## 13. 当前数据注意事项

在进入页面开发前，建议先处理以下数据口径问题：

- `日报/AI工具模型日报 20260611.md` 仍写着 `catalog-data.ts` 待写入，但实际已经写入。
- `来源/官方来源索引 20260611.md` 的来源数量与当前 `catalog-data.ts` 中 `officialSources` 数量不一致；迁移后应以 `src/catalog-data.ts` 为准。
- `推荐/岗位推荐清单 20260611.md` 出现了未在清单中收录的推荐项，应先统一。
- `工具/Agent工具清单 20260611.md` 中 `Vibe` 来源域名与 `catalog-data.ts` 不一致，应统一为 `https://mistral.ai/products/vibe/code/`。

这些问题不阻塞页面结构开发，但会影响页面最终内容可信度。
